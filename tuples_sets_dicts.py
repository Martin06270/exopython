
nombres = (1, 2, 3, 4, 5)
# Tuples are immutable, so you cannot add elements to them.


nombres.add (7)
# Tuples are immutable, so you cannot add elements to them.


print(nombres)
# print(nombres.count(3)) 